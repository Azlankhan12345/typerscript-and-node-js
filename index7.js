function makeShirt(size, text) {
    console.log("You ordered a ".concat(size, " shirt that says ").concat(text));
}
// makeShirt();
// makeShirt('medium')
//different message
makeShirt('medium', 'I need a big shirt to wear');
